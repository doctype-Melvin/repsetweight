{
  "name": "lightweight",
  "version": "1.0.0",
  "description": "Workout logging app using svelte and express",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-backend": "cd backend && npm run serverstart",
    "start-frontend": "cd frontend && npm run dev",
    "dev": "npm-run-all --parallel start-backend start-frontend"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
